# 镜头拆分（分镜生成）性能优化方案

## 背景

当前分镜生成流程：用户点击"生成分镜" → 后台 goroutine → 构建超大 prompt → **单次 LLM 调用** → 解析 JSON → 逐条保存到 DB。用户反馈等待时间过长。

## 当前流水线分析

```mermaid
graph LR
    A["构建 Prompt<br/>~1s"] --> B["单次 LLM 调用<br/>⏱ 主要瓶颈<br/>30s~180s+"]
    B --> C["解析 JSON<br/>~0.1s"]
    C --> D["保存 DB<br/>~1~5s"]
```

### 各阶段耗时分析

| 阶段 | 耗时估计 | 说明 |
|------|---------|------|
| 构建 Prompt | < 1s | DB 查询角色/场景 + 拼接字符串 |
| **LLM 调用** | **30s ~ 180s+** | **单次调用生成全部分镜，token 越多越慢** |
| 解析 JSON | < 0.1s | `SafeParseAIJSON` |
| 保存 DB | 1 ~ 5s | 逐条 [Create](file:///d:/code/github/huobao-drama/application/services/ai_service.go#63-155) + 关联角色 |

> **核心瓶颈：单次 LLM 调用承担了全部工作量**

### 关键问题

1. **Prompt 巨大** — 系统提示 ~110 行 + 用户提示 ~300 行指令 + 完整剧本内容 → 输入 token 可能达到 5000~15000+
2. **单次输出 16000 tokens** — 长剧本要求生成 60~100 个分镜，每个分镜约需 200~400 tokens，总量可达 20000~40000 tokens，**容易超出限制导致截断**
3. **无分段/并行** — 不管剧本多长都是一次性处理
4. **DB 写入串行** — 每个分镜逐条 [Create](file:///d:/code/github/huobao-drama/application/services/ai_service.go#63-155) + 逐条关联角色

---

## 优化策略

### 策略一：快速优化（改动小，效果明显）

#### 1.1 DB 批量写入替换逐条写入

**当前**：每个分镜调用一次 `tx.Create()` + 一次 `tx.Model().Association().Append()`
**优化**：使用 `tx.CreateInBatches(storyboards, 50)` 批量插入 + 批量关联角色

预期效果：DB 写入从 1~5s 降至 < 0.5s

##### [MODIFY] [storyboard_service.go](file:///d:/code/github/huobao-drama/application/services/storyboard_service.go)

[saveStoryboards](file:///d:/code/github/huobao-drama/application/services/storyboard_service.go#763-950) 方法（L763~L948）修改为：
- 先批量构建 `[]models.Storyboard` slice
- 使用 `tx.CreateInBatches(slice, 50)` 一次性写入
- 角色关联改为批量 SQL insert 到关联表

---

#### 1.2 精简 Prompt 中的冗余说明

**当前**：用户 prompt 中包含大量示例、详细格式说明（L138~L322 约 180 行），大部分信息在 system prompt 中已有

**优化**：
- 将重复的格式说明从用户 prompt 移到 system prompt 中（只发一次）
- 精简示例数据（当前两个完整的 JSON 示例占了约 60 行）
- 使用更简洁的约束描述

预期效果：输入 token 减少 30~50%，LLM 响应速度相应提升

##### [MODIFY] [storyboard_service.go](file:///d:/code/github/huobao-drama/application/services/storyboard_service.go)

重构 [GenerateStoryboard](file:///d:/code/github/huobao-drama/api/handlers/storyboard.go#30-68) 中的 prompt 构建逻辑（L128~L322）

---

#### 1.3 提高 Max Output Tokens

**当前**：`ai.WithMaxTokens(16000)`
**优化**：根据剧本长度动态计算合适的 max_tokens

```go
// 按剧本字数估算需要的分镜数，每个分镜平均 300 tokens
estimatedShots := len(scriptContent) / 200  // 约每200字一个分镜  
maxTokens := min(estimatedShots * 350 + 1000, 32000)
```

预期效果：减少因 token 限制导致的截断和重试

---

### 策略二：中期优化（改动中等，效果显著）

#### 2.1 剧本分段 + 多次 LLM 调用

**核心思路**：将长剧本按场景/段落切分为多个"段"，每段独立生成分镜，最后合并。

```mermaid
graph TD
    A["完整剧本"] --> B["按场景切分为 N 段"]
    B --> C1["段1 → LLM 调用"]
    B --> C2["段2 → LLM 调用"]
    B --> C3["段N → LLM 调用"]
    C1 --> D["合并结果"]
    C2 --> D
    C3 --> D
    D --> E["保存 DB"]
```

**实现要点**：
- 按场景变化（地点/时间转换）或按 2000 字为一段切分
- 每段携带上下文摘要（前一段最后 2 个分镜的信息）保持连贯性
- **可选并行**：如果段之间相对独立（不同场景），可以并发调用
- `shot_number` 在合并阶段统一重编号

**风险**：段间分镜连贯性可能有问题（通过上下文传递来缓解）

预期效果：
- 每段 LLM 调用 10~30s（而非 60~180s）
- 串行执行总时间减少 30~50%
- 并行执行总时间减少 50~70%

##### [MODIFY] [storyboard_service.go](file:///d:/code/github/huobao-drama/application/services/storyboard_service.go)

- 新增 `splitScriptIntoSegments(scriptContent string) []string` 方法
- 修改 [processStoryboardGeneration](file:///d:/code/github/huobao-drama/application/services/storyboard_service.go#352-553) 为分段调用逻辑
- prompt 中增加上下文传递参数

---

#### 2.2 前端进度优化

**当前**：前端通过轮询 task 状态获取进度（10% → 50%）
**优化**：分段后每段完成都更新一次进度，提供更细粒度的进度反馈

```
段 1 完成（20%） → 段 2 完成（40%） → 段 3 完成（60%） → 保存 DB（80%） → 完成（100%）
```

---

### 策略三：长期架构优化（改动大，根本解决）

#### 3.1 SSE 实时推送替代轮询

将当前的 task 轮询模式改为 Server-Sent Events (SSE)：
- 后端：Gin 路由 `GET /api/v1/tasks/:id/stream` 使用 `c.Stream()` 
- 前端：`EventSource` 实时接收生成进度和已完成的分镜

#### 3.2 引入消息队列

将分镜生成任务放入消息队列（如 Redis Stream），允许多 worker 消费和分布式扩展。

---

## 推荐实施顺序

> [!TIP]
> 建议先实施 **策略一**（快速优化），预计能将体感等待时间减少 20~40%。如果仍不满意，再实施 **策略二**（分段优化），可进一步减少 50~70%。

| 优先级 | 优化项 | 预期效果 | 开发工作量 |
|--------|--------|---------|-----------|
| P0 | 1.2 精简 Prompt | 输入 token 减少 30~50% | 0.5 天 |
| P0 | 1.3 动态 Max Tokens | 避免截断 | 0.5h |
| P1 | 1.1 DB 批量写入 | DB 阶段提速 5~10x | 0.5 天 |
| P2 | 2.1 剧本分段 + 并行 | 总时间减少 50~70% | 1~2 天 |
| P2 | 2.2 前端进度优化 | 提升用户体验 | 0.5 天 |
| P3 | 3.1 SSE 实时推送 | 消除轮询延迟 | 1 天 |

## Verification Plan

### Automated Tests
```bash
cd d:\code\github\huobao-drama
go test ./application/services/... -v -run TestStoryboard
```

### Manual Verification
1. 准备一个短剧本（~500字）和一个长剧本（~5000字）
2. 分别记录优化前后的生成耗时
3. 验证生成的分镜数量和质量不低于优化前
4. 验证长剧本不再出现 JSON 截断问题
